version: 2

models:
  - name: fct_results
    description: formula 1 results fact table
    columns:
      - name: result_id
        description: unique id of fct_results
        tests:
          - unique
          - not_null
      